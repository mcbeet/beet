data_pack:
  load:
    # mount all .mcfunction files in "src"
    data/demo/function: "src"
    # put gcd.mcfunction and is_prime.mcfunction in "math_util"
    data/demo/function/math_util: ["vendor/gcd.mcfunction", "vendor/is_prime.mcfunction"]
    # add schedule_clock.mcfunction as main.mcfunction
    data/demo/function/main.mcfunction: "vendor/schedule_clock.mcfunction"

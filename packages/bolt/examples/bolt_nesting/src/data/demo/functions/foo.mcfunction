from contextlib import contextmanager
from bolt import Runtime

runtime = ctx.inject(Runtime)
say runtime.nesting

@contextmanager
def stuff():
    if score foo temp matches 77:
        yield

as @s say runtime.nesting

name = ./thing

with stuff():
    function name:
        say runtime.nesting
    say runtime.nesting

say runtime.nesting
